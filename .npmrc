# electron_mirror was here but it duplicates the npm default and triggers
# "Unknown project config" warnings in npm 10+. Removed — electron-builder
# falls back to the same GitHub URL automatically.
